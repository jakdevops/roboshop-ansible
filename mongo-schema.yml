- name: copy the mongodb repo file
  ansible.builtin.copy:
    src: mongo.repo
    dest: /etc/yum.repos.d/mongo.repo

- name: install mongdb client
  ansible.builtin.yum:
    name: mongodb-org-shell
    state: installed

- name: load schema
  ansible:builtin.shell: mongo --host mongodb.jakdevops.online </app/schema/${{component}}.js